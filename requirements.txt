# Django核心框架（指定稳定版本，和本地开发一致即可）
Django==5.0.7
# 静态文件/图片处理必备
Pillow==10.4.0
# 数据库迁移/适配（Django默认自带，指定版本防冲突）
sqlparse==0.5.1
# 表单验证/美化（可选，若项目用到则加）
django-crispy-forms==2.1
crispy-bootstrap5==0.8.1
# 密码加密/安全相关（Django依赖，自动安装，显式指定更稳定）
bcrypt==4.1.3
cryptography==42.0.8
# PythonAnyWhere部署必备（解决WSGI/环境适配）
gunicorn==22.0.0